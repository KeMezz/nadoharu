## 1. 라우트/콜로케이션 골격 및 GraphQL 문서 준비

- [ ] 1.1 `app/posts`와 `app/posts/[id]`에 `_components` 기반 라우트 구조를 추가한다.
- [ ] 1.2 타임라인/상세/작성/수정/업로드용 `*.query.ts`, `*.mutation.ts`, `*.graphql` 파일을 라우트 인접 위치에 생성한다(수정 폼 `/posts/[id]/edit`용 기존 게시물 조회 `post(id)` 쿼리 포함).
- [ ] 1.3 `pnpm --filter web graphql:generate`를 실행해 near-operation-file 생성 타입(`*.generated.ts`)을 반영한다.

## 2. 인증 경계와 라우트 정책 반영

- [ ] 2.1 (RED) `/posts` 비인증 접근 시 로그인 안내 및 `posts` 쿼리 미호출, `/posts/[id]` 비인증 접근 허용, `/posts/new` 및 `/posts/[id]/edit` 비인증 접근 시 `/login` 리다이렉트 시나리오 테스트를 먼저 작성한다.
- [ ] 2.2 라우트 가드 정책을 `/me` 보호, `/posts` 접근 허용+안내, `/posts/[id]` 공개 조회, `/posts/new`·`/posts/[id]/edit` 보호로 수정한다.
- [ ] 2.3 `/posts` 화면에 로그인 이동 버튼을 구현하고 `/login` 이동 동작을 연결한다.

## 3. `/posts` 타임라인 무한 스크롤 구현

- [ ] 3.1 (RED) 초기 로드, `hasNextPage=false` 중단, 중복 요청 방지, 다음 페이지 실패 시 재시도 가능 상태 표시 시나리오의 단위 테스트를 먼저 작성한다.
- [ ] 3.2 `posts(first, after)` 기반 커서 훅/쿼리를 구현하고 `endCursor`로 다음 페이지를 이어붙인다.
- [ ] 3.3 최신순 고정(`createdAt DESC`, `id DESC`) 전제를 반영하고 정렬 변경 UI를 노출하지 않는다.
- [ ] 3.4 타임라인 로딩/빈 상태/오류 상태 UI를 구현하고, 다음 페이지 실패 시 기존 목록 유지 + 재시도 액션을 제공한다.

## 4. `/posts/[id]` 공개 단건 상세 구현

- [ ] 4.1 (RED) 비인증 접근 성공, 존재하지 않는 ID, 일시적 오류 재시도 시나리오 테스트를 먼저 작성한다.
- [ ] 4.2 단건 조회 쿼리와 상세 렌더링 컴포넌트를 구현한다.
- [ ] 4.3 실패 상태에서 재시도 또는 목록 이동 액션을 제공한다.

## 5. 작성/수정 폼과 카테고리 임시 정책 반영

- [ ] 5.1 (RED) 폼 검증/제출 상태 전이, 최소 콘텐츠(`content` trim 값 또는 `imageUrls` 1개 이상) 검증, `content`/`subcontent` 150자 사전 검증, 수정 폼 데이터 로드 실패(존재하지 않는/삭제된 게시물, 네트워크 오류) 및 비작성자 접근 조기 차단 시나리오, 카테고리 UI 미노출, 카테고리 빈 값 고정 시나리오 테스트를 먼저 작성한다.
- [ ] 5.2 `use<PostForm>Form` 훅으로 검증/제출/에러 매핑/리다이렉트 로직을 UI에서 분리한다.
- [ ] 5.3 작성/수정 화면에서 카테고리 입력 컨트롤을 제거하고 제출 payload의 카테고리를 빈 값으로 고정한다.
- [ ] 5.4 작성/수정 요청 실패 시 GraphQL `errors[].extensions.code` 기준 인증/인가 오류를 구분해 로그인 유도 또는 권한 없음 메시지를 표시한다.
- [ ] 5.5 수정 폼 진입 시 기존 게시물 데이터를 프리필하고, 로드 실패 시 오류 상태+목록 이동(네트워크 오류는 재시도 포함)을 제공하며, 비작성자 접근은 권한 없음 안내 후 `/posts/[id]`로 조기 이동 처리하고, 수정 제출 시 `imageUrls` 최종 목록 전체를 전달해 전체 교체 시맨틱을 보장한다.

## 6. presigned URL 이미지 업로드 흐름 구현

- [ ] 6.1 (RED) 파일 포맷/용량 사전 검증, presigned URL 발급 실패, 업로드 실패 후 재시도 시나리오 테스트를 먼저 작성한다.
- [ ] 6.2 `packages/shared`에 업로드 허용 포맷/최대 용량 정책을 정의하고 웹/서버 연동 지점을 맞춘다(백엔드 API 계약 변경 없이 정책 상수/검증 기준만 정렬).
- [ ] 6.3 presigned URL 발급 후 스토리지 업로드 2단계 흐름을 구현하고 성공 시 폼 이미지 상태를 갱신한다.
- [ ] 6.4 presigned URL 발급 단계와 스토리지 업로드 단계의 실패 원인 메시지를 구분하고 재시도 액션을 구현한다.

## 7. 테스트 및 최종 검증

- [ ] 7.1 Vitest로 타임라인/상세/폼/업로드/권한 분기/수정 폼 프리필·로드 실패 처리·비작성자 조기 차단·이미지 전체 목록 제출 테스트를 통과시킨다.
- [ ] 7.2 Playwright로 비인증 `/posts` 안내, 비인증 `/posts/new`/`/posts/[id]/edit` 리다이렉트, 타임라인 추가 로드, 다음 페이지 실패 후 재시도, 비인증 `/posts/[id]` 조회, 작성/수정 성공 후 `/posts/[id]` 이동 시나리오를 통과시킨다.
- [ ] 7.3 `pnpm --filter web lint`와 `pnpm --filter web test`로 최종 회귀를 확인한다.
